# secretos y estado local
.env
terraform.tfvars
*.auto.tfvars
*.tfstate
*.tfstate.*
.terraform/
crash.log
crash.*.log

# .terraform.lock.hcl SI se commitea: es el pin de versiones+hashes de los providers.

# artefactos generados a partir de templates
config/servertest.ini
data/
backups/
*.log

# binarios locales (mcrcon compilado)
bin/
